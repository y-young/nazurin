[tool.ruff]
target-version = "py38"

[tool.ruff.lint]
extend-select = [
  "B", # flake8-bugbear
  "FBT", # flake8-boolean-trap
  "I", # isort
  "N", # pep8-naming
  "PL", # pylint
  "UP", # pyupgrade
]
ignore = ["UP007"]